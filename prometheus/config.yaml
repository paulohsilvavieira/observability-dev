global:
  scrape_interval: 1s  # Define a frequência de coleta das métricas

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel:9464"]  # Pegando métricas do OpenTelemetry Collector
